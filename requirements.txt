# ── NRM Bot — runtime dependencies ──────────────────────────────
# Pinned to minor versions for reproducible Railway deploys.
# Run:  pip install -r requirements.txt

# Web dashboard & API
Flask==3.0.3
gunicorn==23.0.0

# Exchange connectivity
websocket-client==1.8.0
requests==2.32.3

# AI validator / coach (Claude)
# Note: anthropic must be a modern release — 0.39 broke against current httpx
# (the `proxies` kwarg was removed from httpx.Client).
anthropic==0.69.0

# Auth (password hashing)
bcrypt==4.2.0

# At-rest encryption for dashboard-entered Bitget API keys (secrets_vault.py)
cryptography==43.0.1

# News briefing feeds
feedparser==6.0.11

# Scheduled briefings (analysis_agent)
schedule==1.2.2

# PostgreSQL driver (Railway)
# 2.9.10 ships cp313 manylinux wheels (with bundled libpq). 2.9.9 had no 3.13
# wheel, so under the Railpack builder (Python 3.13) it compiled from source and
# linked the build-stage libpq — absent in the runtime image, so `import psycopg2`
# failed at runtime ("psycopg2 not installed — DB disabled"). The wheel fixes it.
psycopg2-binary==2.9.10


# ── Build-time only (knowledge base) ────────────────────────────
# These are needed locally to (re)build knowledge_base.json from PDFs,
# NOT on the Railway runtime. Install on demand:
#   pip install -r requirements-build.txt
#
# pdfplumber==0.11.4
# chromadb==0.5.23


# ── Local research / backtesting (optional) ─────────────────────
# Used by standalone backtest scripts, not the live bot:
#   pip install ccxt
#
# ccxt==4.4.0
